{{ form_start(eventSearchForm, {'action': path('searchEvents'), 'method': 'POST','attr':{'class':'navbar-form'} }) }}
                <div class="input-group header__searchInput">
                    {{ form_widget(eventSearchForm.title, {'attr':{'class':'form-control', 'placeholder':'user.searchEvent'|trans}}) }}
                    <div class="input-group-btn">
                        <button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-search "></span></button>
                    </div>
                </div>
                <div class="form-group header__cityDropdown">
                    {{ form_widget(eventSearchForm.city, {'attr':{'class':'form-control'}, 'placeholder' : 'Miestas'}) }}
                </div>
{{ form_widget(eventSearchForm) }}
{{ form_end(eventSearchForm) }}



