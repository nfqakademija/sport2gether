{% extends "::base.html.twig" %}

{% block title %}AppBundle:sportEvent:createEvent{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Event</h1>
                {{ form_start(eventForm) }}
                {{ form_widget(eventForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(eventForm) }}
            </div>
        </div>
    </div>
{% endblock %}
